# Milestone 2: noise gate, compressor, and parametric EQ modules live here,
# each exposing a callable(indata: np.ndarray) -> np.ndarray so they can be
# chained and passed as the `processor` argument to audio.engine.AudioEngine.
